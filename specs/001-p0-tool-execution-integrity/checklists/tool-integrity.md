# Tool Integrity Requirements Quality Checklist: P0 Tool Execution Integrity Remediation

**Purpose**: Validate that the *requirements* governing tool authorization, unknown-tool handling,
stub collisions, sandbox bypasses, false-success prevention, execution evidence, backward
compatibility and audit traceability are complete, unambiguous, consistent and measurable — before
implementation begins.

**Created**: 2026-08-29
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [contracts/](../contracts/)
**Depth**: Release gate (the audit's verdict is "do not release"; Constitution II and III are
fail-closed gates)
**Audience**: Reviewer, pre-implementation

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

**This checklist tests the requirements, not the system.** Every item asks whether something is
*written well enough to build against*, never whether the built thing works.

---

## Tool Authorization

- [ ] CHK001 Are the requirements for *how* an operator configures the mount allowlist specified — file, environment, per-package or per-deployment? [Gap, Spec §FR-014]
- [ ] CHK002 Is authorization defined for the **tool itself**, or only for its mounts? Constitution II requires a tool be "explicitly authorized"; FR-014 to FR-017 govern mounts exclusively [Gap, Conflict, Spec §FR-014–FR-017]
- [ ] CHK003 Are requirements defined for who counts as the "operator", and how that role is distinguished from the agent at configuration time? [Clarity, Spec §Assumptions]
- [ ] CHK004 Is the precedence between an operator allowlist entry and the dangerous-location floor stated as a requirement, not only in a design contract? [Traceability, Spec §FR-016, contracts/execution-policy.md]
- [ ] CHK005 Are requirements specified for what happens when the allowlist configuration is itself malformed or unreadable? [Gap, Edge Case]
- [ ] CHK006 Is the criterion for classifying a tool as RISKY versus SAFE defined anywhere in the requirements, or only asserted per-tool in the data model? [Gap, Spec §FR-008, data-model.md §1]

## Unknown & Invalid Tool Handling

- [ ] CHK007 Does FR-003's "reject **or** explicitly surface" specify which of the two applies, and under what condition? These are materially different behaviours [Ambiguity, Spec §FR-003]
- [ ] CHK008 Is "the point the declaration is recorded" defined against concrete lifecycle stages — compose, refine, build, load, preflight, run? [Clarity, Spec §FR-002]
- [ ] CHK009 Are requirements consistent about whether an unknown tool fails at build time, run time, or both? FR-002/FR-003 imply authoring-time; FR-037 states validation *and* run [Consistency, Spec §FR-002, §FR-003, §FR-037]
- [ ] CHK010 Are the five rejection categories — unknown, invalid, unresolvable, unauthorized, unsafe — each given a distinguishing definition, or are they used as an undifferentiated list? [Clarity, Spec §FR-037, §FR-039]
- [ ] CHK011 Are requirements defined for a hand-edited or third-party package whose tool declarations were never produced by the factory? [Gap, Coverage]
- [ ] CHK012 Is the required content of a rejection message specified — which fields must appear, and in what form? [Measurability, Spec §FR-003, §FR-037]
- [ ] CHK013 Are requirements defined for whether a rejection is reported once per offending declaration or aggregated, following the existing collect-don't-short-circuit convention? [Consistency, Gap, Spec §FR-023]

## Stub Collisions & Definition Uniqueness

- [ ] CHK014 Is "exactly one definition per tool name" scoped to a module, a package, or the whole generated tree? [Clarity, Spec §FR-006]
- [ ] CHK015 Are FR-010 (no implementation = build failure) and the conditionally-registered tools reconciled? Several catalog tools register only when an optional dependency or key is present [Conflict, Spec §FR-010, §FR-030]
- [ ] CHK016 Is a canonical tool that is unavailable *in this environment* classified as "unresolvable" (FR-023) or "no implementation" (FR-010)? The requirements permit both readings [Ambiguity, Spec §FR-010, §FR-023]
- [ ] CHK017 Are requirements defined for the relationship between the registry key, the catalog key and the agent-facing tool name, or is single-sourcing stated only as a design decision? [Traceability, Spec §FR-007, research.md D-2]
- [ ] CHK018 Are requirements specified for what a generated package must contain when the team declares zero tools? [Coverage, Gap, Spec §FR-049]

## Sandbox & Execution-Policy Bypass Prevention

- [ ] CHK019 Do the requirements state whether mandatory sandboxing applies to the product run path, the standalone generated package, or both? [Clarity, Gap, Spec §FR-012, §FR-025]
- [ ] CHK020 Is "the sandbox cannot be established" defined with enumerated conditions, or left to implementation judgement? [Measurability, Spec §FR-013]
- [ ] CHK021 Are requirements defined for sandbox **network egress** policy? The existing template carries a network setting the requirements never mention [Gap, Coverage]
- [ ] CHK022 Are execution timeout requirements specified? The current implementation uses three different timeouts and the requirements are silent [Gap, Non-Functional]
- [ ] CHK023 Are requirements defined for sandbox resource limits — memory, CPU, disk — or is isolation assumed to be filesystem-only? [Gap, Non-Functional]
- [ ] CHK024 Is the dangerous-location floor stated in the requirements as extendable-but-not-reducible, or does that property live only in a contract and an assumption? [Traceability, Spec §FR-016, §Assumptions]
- [ ] CHK025 Are requirements defined for what a SAFE-classified tool is permitted to do, or is the policy specified only for RISKY tools? [Coverage, Gap, Spec §FR-008]
- [ ] CHK026 Do the requirements address a sandbox that becomes unavailable **mid-run**, distinctly from one unavailable at start? [Edge Case, Spec §FR-013, §Edge Cases]

## False-Success Prevention & Completion Semantics

- [ ] CHK027 Is "declares an external capability" defined? Whether internal tools such as shared-state readers count is decisive for the completion rule and is not stated [Ambiguity, Spec §FR-027]
- [ ] CHK028 Is the all-or-nothing reading of FR-027 intended — a task declaring three tools needs three receipts — or is one sufficient? An agent may legitimately not need every declared tool [Ambiguity, Conflict, Spec §FR-027]
- [ ] CHK029 Are requirements defined for how an unevidenced completion is surfaced to the user, as distinct from a run error? [Gap, Spec §FR-028]
- [ ] CHK030 Are requirements specified for a run cancelled or errored mid-flight, where some receipts exist? The edge case is listed but no FR governs it [Traceability, Gap, Spec §Edge Cases]
- [ ] CHK031 Is the completion rule's behaviour specified when a declared tool executed for a *different* task than the one declaring it? [Coverage, Spec §FR-027, data-model.md §7]
- [ ] CHK032 Are requirements defined for whether an unevidenced run blocks downstream consumers — API response shape, exit code, UI state — or only annotates the result? [Gap, Dependency]

## Execution Evidence & Receipt Quality

- [ ] CHK033 Is "output reference" defined — what it points at, and whether the target must remain retrievable? [Clarity, Spec §FR-026]
- [ ] CHK034 Are receipt persistence and retention requirements specified, or are receipts assumed to live only for the duration of the run result? [Gap, Dependency]
- [ ] CHK035 Is "sanitized input" defined by a stated rule, or only by reference to an existing implementation guard? [Measurability, Spec §FR-026, §FR-029, research.md D-5]
- [ ] CHK036 Are requirements defined for receipt behaviour under concurrent runs, given the documented process-wide run lock and shared event bus? [Gap, Coverage]
- [ ] CHK037 Are requirements specified for the ordering guarantees of receipts relative to transcript entries? [Gap, Clarity]
- [ ] CHK038 Can "verified execution receipt" be objectively distinguished from a model-asserted claim by the requirement text alone? [Measurability, Spec §FR-026, §Assumptions]

## Backward Compatibility & Migration

- [ ] CHK039 Are FR-038 (safely-resolving packages unchanged) and FR-030 (validation checks declared capability exists) consistent for a package whose tools are canonical but environment-unavailable? [Conflict, Spec §FR-030, §FR-038]
- [ ] CHK040 Are requirements defined for whether the runtime must continue to load packages generated by the *pre-remediation* template, whose module shape differs? [Gap, Coverage, Spec §FR-047]
- [ ] CHK041 Is the scope of "unchanged behaviour" for safely-resolving packages defined — identical outputs, identical artifacts, or merely successful execution? [Measurability, Spec §FR-038, §FR-047]
- [ ] CHK042 Are requirements specified for regenerating an existing package, as opposed to running it as-is? [Gap, Coverage]
- [ ] CHK043 Is the migration report's output format specified well enough to be consumed programmatically, or only described narratively? [Clarity, Spec §FR-039–FR-042]
- [ ] CHK044 Are requirements defined for how a user confirms a migrated package now passes, given the report is advisory and rewrites nothing? [Gap, Recovery Flow]

## Audit-Finding Traceability

- [ ] CHK045 Is the authoritative audit ID scheme stated? The requirements cite RC-n and P0-n, while the source audit also carries persona finding IDs [Ambiguity, Spec §SC-013]
- [ ] CHK046 Are requirements defined for what constitutes exercising a finding's "original reproduction", given several audit findings cite transcripts rather than runnable commands? [Measurability, Spec §SC-013, Constitution IV]
- [ ] CHK047 Is every in-scope root cause (RC-3, RC-4, RC-5, RC-8, RC-10, RC-11, RC-12) traceable to at least one functional requirement, with no orphan RC and no untraced FR? [Completeness, Traceability, Spec §Source of truth]
- [ ] CHK048 Are the requirements explicit that the out-of-scope shared-memory gap shares RC-5's mechanism but is *not* closed by this feature, so a reviewer cannot mistake RC-5 for fully remediated? [Clarity, Spec §Scope, §Assumptions]

## Cross-Cutting Ambiguities & Conflicts

- [ ] CHK049 Are requirements defined for whether refusal diagnostics may contain host filesystem paths, given a rejected mount must be named (FR-017) and those messages may reach an API or UI? [Gap, Security, Conflict with §FR-029]
- [ ] CHK050 Is the destination of refusal diagnostics specified — user-facing, log, or both? Constitution II requires refusals be observable; the requirements say only that they "name" the condition [Gap, Clarity, Spec §FR-009, §FR-013, §FR-017]
- [ ] CHK051 Are the atomicity requirements of FR-018 expressed in terms a reviewer can objectively enforce at merge time? [Measurability, Spec §FR-018]
- [ ] CHK052 Is the assumption that the source audit is accurate as to mechanism recorded as a validated dependency, given two of its implementation pointers were found incorrect during planning? [Assumption, Spec §Assumptions, research.md C-1, C-2]

---

## Triage Status (2026-08-29, post-analysis)

All 52 items triaged. **No checkbox has been marked** — resolution status is recorded here so the
reviewer can evaluate each claim against the amended artifacts and decide. An item is listed as
Resolved only with a specific spec, plan or data-model citation.

### Resolved by Amendments 1-6 (15)

| Item | Citation |
|---|---|
| CHK002, CHK003 | FR-050 to FR-055 — three necessary conditions; declaration is not authorization |
| CHK006 | FR-083 + T014 — RISKY criterion stated and its inverse enforced |
| CHK007, CHK008, CHK009 | FR-003 amended; FR-056 to FR-058 — stage-deterministic |
| CHK012 | FR-060 — rejection names tool, agent, stage, reason class |
| CHK015, CHK016 | FR-065 to FR-069 — known-but-unavailable is a distinct state |
| CHK021, CHK022, CHK023 | FR-073 to FR-078 + data-model.md §10 defaults table |
| CHK027, CHK028 | FR-061 to FR-064 — required vs available; successful receipt for claims |
| CHK049 | FR-070 to FR-072 — safe alias, no raw paths in receipts or user-facing errors |

### Resolved by Amendment 7 (7)

| Item | Citation |
|---|---|
| CHK001, CHK005 | FR-085 + T054 — single operator config source, credential-file precedence, unreadable denies and diagnoses |
| CHK004, CHK024 | FR-079 + T067 — deny floor stated as a requirement, extendable never reducible |
| CHK011 | FR-080 + T108 — hand-edited packages take the identical preflight gate |
| CHK019 | FR-081 + T062 — sandboxing applies to product run path and standalone package alike |
| CHK020, CHK026 | FR-082 + T061 — six enumerated conditions including mid-run loss |
| CHK025 | FR-083 + T089 — SAFE tool boundary defined and regression-tested |
| CHK040 | FR-084 + T082, T112 — pre-remediation module shape detected and refused |

### Resolved by clarification of existing requirements (10)

| Item | Citation |
|---|---|
| CHK010, CHK013 | FR-060 amended — five reason classes each defined; rejections aggregate |
| CHK014 | FR-006 amended — one definition per tool **per generated module** |
| CHK017 | FR-007 amended + T035 — registry key, agent-facing name and catalog name are one string |
| CHK031 | T124 — receipts for a non-declaring task are ignored by the rule |
| CHK033, CHK035 | FR-026 amended — output reference and sanitized input both defined |
| CHK036 | spec Assumptions — the existing process-wide run lock serializes runs |
| CHK037 | spec Assumptions + T125 — receipts follow the sparse-sequence convention |
| CHK041 | FR-047 amended — "unchanged behaviour" defined, excluding byte-identical output |
| CHK051 | T096 — FR-018 atomicity enforced by an objective merge gate |

### Resolved by verification against existing artifacts (5)

| Item | Verification |
|---|---|
| CHK038 | Receipts derive from runtime `ToolUsage*` events, never model self-report — research.md D-5, contracts/receipts-and-completion.md |
| CHK042 | Regeneration is a build, so FR-057 and FR-030 already govern it |
| CHK044 | Re-running validation confirms a migrated package passes — FR-030, T140 |
| CHK045 | Audit ID scheme stated in spec Source of truth and the tasks.md legend |
| CHK046 | Exercising an original reproduction is defined by the Phase 1 evidence files and T142 |
| CHK047 | Verified: RC-3→FR-001, RC-4→FR-006, RC-5→FR-019, RC-8→FR-030, RC-10→FR-008, RC-11→FR-026, RC-12→FR-034; no orphan RC |
| CHK048 | spec Scope and Assumptions both state the shared-memory gap is not closed by RC-5's fix |
| CHK052 | research.md C-1 and C-2 record the two incorrect audit pointers; spec Assumptions carries the validated-dependency note |

### Deferred — non-blocking, with reason and destination (5)

| Item | Reason it does not block | Destination |
|---|---|---|
| CHK018 | A zero-tool package's artifact content is unchanged by this feature; FR-049 covers behaviour | Follow-up codegen tidy |
| CHK029, CHK032 | How an unevidenced completion surfaces in the API/UI, and whether it blocks downstream consumers, is an API and frontend concern. This feature touches neither `api/` nor `web/` (plan.md Project Type). FR-028 already puts the data on `RunResult` | Follow-up spec for the API/UI surface |
| CHK030 | A run cancelled mid-flight retains its receipts and reports incomplete; the edge case is recorded and FR-027 already refuses success without evidence | Follow-up if a cancellation UX is specified |
| CHK034 | Receipt persistence beyond the run result is a storage concern; spec Assumptions records the in-memory lifetime | Follow-up spec alongside CHK029/032 |
| CHK043 | The migration report's machine-readable format is an implementation choice inside T038 with no security consequence | Decide during Phase 3 implementation |

### Security items — all resolved before implementation

Per the amendment brief, no security ambiguity is deferred. CHK002, CHK003, CHK004, CHK005, CHK006,
CHK011, CHK019, CHK020, CHK021, CHK022, CHK023, CHK024, CHK025, CHK026, CHK040 and CHK049 are all in
the Resolved sections above, each with a requirement and an implementing task.

**Tally**: 37 resolved with citations · 5 deferred with destinations · 10 previously resolved and
re-confirmed. 0 security items deferred.

---

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- Items marked `[Conflict]` or `[Ambiguity]` describe requirement pairs that cannot both be satisfied
  as written, or text with two defensible readings. These warrant a spec amendment rather than an
  implementation decision — resolving them silently in code is how RC-3 and RC-10 originated
- Items marked `[Gap]` may be legitimate deferrals. Recording the deferral in the spec is what makes
  it a decision rather than an omission
- 52 items; 50 carry an explicit spec reference or a `[Gap]`/`[Ambiguity]`/`[Conflict]`/`[Assumption]`
  marker (96% traceability)
