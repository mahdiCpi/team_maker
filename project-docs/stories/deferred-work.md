# Deferred Work

## Deferred from: reconciliation (2026-07-12)

See [reconciliation-notes.md](reconciliation-notes.md) for full context. The `guru-explore` merge
introduced a temporary **provider/key split-brain**: `team_maker/keyconfig.py` +
`team_maker/providers/registry.py` (Story 1.1) coexist with `team_maker/llm/model_resolver.py`
(availability/substitution). Both retained deliberately; unification is tracked as **Story 0.4** and
the broader spine reconciliation as **Epic 0** (Stories 0.1–0.5). Not deferred indefinitely — this is
the next architectural work item, ahead of new Epic 1 features.

## Deferred from: code review of story-1.1 (2026-07-11)

- **Duplicate key definitions silently resolve last-wins** — if the Key Config has the same provider twice (e.g. `ANTHROPIC_API_KEY=` and `anthropic=`), the later value wins with no warning. Acceptable for now; revisit if it causes confusion.
- **Key supplied for a keyless-local provider is ignored** — `config.has("ollama")` can be True but `report_availability` reports `keyless-local` regardless (keyless branch checked first). Harmless today; reconcile if local providers ever take optional auth.
- **OpenRouter gateway identified by a hardcoded name** — `OPENROUTER = "openrouter"` is used in logic rather than a `is_gateway` data flag on `Provider`. Fine while OpenRouter is the only gateway (AD-8 names it); convert to a data flag if a second gateway provider is added.

## Deferred from: code review of story-0.3 (2026-07-18)

- `get_runtime_engine`'s silent fallback-to-crewai (`team_maker/adapters/runtime_engines/__init__.py:13-14`)
  combined with `_render_requirements`'s raw `framework == "crewai"` string check
  (`team_maker/pipeline/runner.py:304-307`) can diverge for an unrecognized `framework` value: the
  requirements list would omit `litellm>=1.0` even though the engine that actually renders
  `run_example.py` is CrewAI's (via the fallback). Pre-existing since before Story 0.3 — the old
  `get_adapter`/`framework_deps.get(framework, framework_deps["crewai"])` had byte-identical
  behavior. Not fixed in Story 0.3 because its Dev Notes explicitly direct keeping this branch
  "exactly as-is" (behavior-preserving refactor scope). Candidate fix: branch on `adapter.name`
  instead of the raw `framework` string, and/or log a warning on fallback in `get_runtime_engine`.

## Deferred from: code review of story-0.4 (2026-07-25)

- The four model-list fetchers' `lru_cache`s (`_anthropic_models`, `_openai_models`, `_xai_models`,
  `_google_models` in `team_maker/llm/model_resolver.py:39-97`) now key on the raw resolved secret
  value instead of the env-var name, so plaintext API keys are retained indefinitely in process
  memory for the lifetime of the process (no eviction, no rotation-awareness). This is a deliberate,
  spec-mandated design from Story 0.4's Task 3 ("the `@lru_cache` still works: it now caches per
  key-value instead of per-env-var-name, which is equivalent or better") — not fixed in the story's
  code review because overriding an explicit story decision mid-review would exceed the review's
  scope. Candidate fix: cache by a hash (e.g. `hashlib.sha256`) of the key instead of the raw value,
  or swap `lru_cache` for a cache the caller can explicitly evict on key rotation.

## Deferred from: code review of story-1.2 (2026-07-26)

- **`compose --build` doesn't bridge per-role provider keys** — only the authoring provider's key is bridged into `os.environ` before `composer.compose()` runs; if the composed spec routes roles to `openai`/`google`/`xai` (per the Composer's own word→provider alias table), nothing bridges those keys before `PipelineRunner().run()` executes. Harmless today — `model_resolver`'s live-model-substitution already degrades gracefully with "API unreachable — trust the YAML" when a key is absent — and run-time key gating for non-authoring providers is explicitly Story 1.6/FR-10 scope.
- **Generic exception printing in `compose` could echo SDK-embedded secrets** — `except Exception as exc: ... escape(str(exc))` around `composer.compose(intent)` prints the raw exception string, which could in theory contain credential fragments from an underlying SDK error. Matches the existing `create` command's identical pattern elsewhere in `team_maker/cli.py`; not a new regression, and there's no established scrubbing convention in the codebase to extend.
- **`_PROVIDER_ALIASES` is prompt-only, not code-enforced** — a malformed provider word from the authoring LLM costs one extra validate-and-repair round-trip rather than being silently corrected in code. Already covered by the existing AC3 repair loop; an efficiency nit, not a correctness gap.
- **No retry on non-`ValidationError` failures in `Composer.compose`'s repair loop** — transient/network/parsing failures propagate immediately with zero retry. AC3 explicitly scopes the retry budget to schema-validation failures, not general resilience; revisit if flaky LLM calls become a real-world problem.
- **No repair-attempt progress feedback in `compose`'s non-quiet output** — a UX nicety (e.g. "repair attempt 2/3"), not required by any AC.
- **`compose --model` cannot override the authoring provider, only the model string** — matches Task 4's literal spec (only `--model` was requested); revisit if a `--provider` override becomes a real user need.

## Deferred from: code review of story-1.3 (2026-07-27)

- **Only `ComposerError` is caught around `session.refine()` in the interactive loop** — any other exception (network/transient/parse failure) is uncaught there and crashes the whole conversation (exit 1) instead of letting the user retry that turn. Consistent with Story 1.2's own established precedent that retries are scoped to schema-validation failures only, not general resilience.
- **No cap on conversation turns or consecutive failed-refinement attempts** in `compose --interactive` — a confused user or scripted stdin can drive unbounded LLM spend with zero guardrail.
- **No `KeyboardInterrupt` handling in the interactive loop** — Ctrl+C while blocked on `input()` produces a raw traceback; no existing convention for this elsewhere in the CLI either.
- **`ComposerSession` has no undo/rollback** beyond asking the LLM to revert a change in a further turn. AD-11 only requires in-memory current state for v1; no history/versioning requirement in Story 1.3's ACs.
- **Prompt/token cost grows with spec size on every refinement turn** — the full current spec is re-embedded on each `refine()` call with no truncation, diffing, or summarization strategy. A real scalability concern for larger specs or longer conversations, not required by this story.
- **"Apply only this change" is prompt-only guidance, never code-verified** — nothing diffs the LLM's output against the prior spec to catch silent drift in fields the user didn't ask to change. Matches Story 1.2's precedent that prompt-level LLM guidance is acceptable and only schema-level issues are code-enforced via the repair loop.

## Deferred from: code review of story-1.4 (2026-07-27)

- **No `--quiet` coverage in `tests/unit/test_cli_create.py`** (alone or combined with a failing validation result) — valid coverage gap, but not one of Story 1.4 Task 2's explicitly enumerated scenarios.
- **No coverage of the `create` command's override flags** (`--output`/`-o`, `--framework`, `--state-backend`, `--planner-model`, `--no-planner`) in the new CLI tests — real gap, out of Task 2's explicit scope.
- **No negative test for a config file that fails to *parse* as YAML** (distinct from a schema-invalid-but-parseable file) — a different code path (`load_yaml`'s exception handler, `team_maker/cli.py:105-109`) than the schema-invalid test Task 2 asked for.
- **All four `test_cli_create.py` tests share a payload with non-empty `desired_roles`**, so the LLM-planner code path is never exercised by that file; a future edit to the shared `_valid_payload` helper that drops `desired_roles` could accidentally route a test through a real LLM call. Speculative future-hardening, not a current defect.
- **No test for a clean build (exit 0) with warnings but zero issues** (e.g. the "No tasks were generated" warning on its own) in `test_cli_create.py` — valid coverage idea, not one of Task 2's explicitly enumerated scenarios.

## Deferred from: story-1.5 implementation (2026-07-31)

- **The generated `run_example.py` template (`team_maker/codegen/templates/crewai_runner.py.j2:156-163`) builds a broken hierarchical `Crew` for any team with an orchestrator agent** — it passes the manager agent in both `agents=` and `manager_agent=`, which real CrewAI (confirmed on 1.14.5, installed for the first time in this repo as part of Story 1.5) rejects outright: `pydantic_core.ValidationError: Manager agent should not be included in agents list`. This means the standalone `python run_example.py` path has always been broken for any orchestrator/hierarchical team — undiscovered until now because CrewAI was never actually installed anywhere in this repo before Story 1.5. Story 1.5's own new in-process `CrewAIExecutionEngine` (`team_maker/adapters/runtime_crewai/`) works around this correctly (excludes the manager from `agents`), but the template itself is untouched — fixing generated-package code is outside Story 1.5's scope (it doesn't touch the Factory/codegen layer). Candidate fix: in `crewai_runner.py.j2`, exclude `ORCHESTRATOR_ROLE` from the `agents=list(agents.values())` passed to `Crew(...)` when `process=Process.hierarchical`.

## Deferred from: code review of story-1.5 (2026-07-31)

- **Multiple `is_orchestrator=True` agents in a package are not detected** — `CrewAIExecutionEngine._build_crew` picks the first via `next(...)`; a second orchestrator silently becomes a non-manager worker with `allow_delegation=True`, with no error or warning that the team's data was ambiguous. Genuinely uncertain whether the Factory can produce this today; no AC dictates the "correct" behavior (error vs. warn vs. current silent-first).
- ~~**`pyproject.toml`'s `crewai>=0.80.0` (in the `runtime`/`all`/`dev` groups) has no upper version bound**~~ — **resolved by Story 1.6 (2026-08-02):** pinned to `>=1.14.6,<1.15`, gated on `tests/conformance/test_multi_provider_conformance.py`. Note the *generated* package's own pin (`adapters/runtime_engines/crewai_engine.py`'s `extra_requirements()`) is still unbounded — that is codegen output, outside Story 1.6's scope.
- ~~**An unrecognized/misspelled LLM provider name in `_build_llm` degrades silently** to `api_key=None`~~ — **resolved by Story 1.6 (2026-08-02):** the pre-run gate (`runtime/preflight.py`) now aborts naming the unrecognized provider and listing the valid ones.
- **No validation that `run`'s `goal` argument is non-empty** before being passed to `crew.kickoff(inputs={"goal": goal})` — CrewAI will simply process an empty goal rather than crash, so this is a UX nicety, not a correctness bug.
- ~~**`team-maker run`'s `--key-file` resolved path is never echoed back in its output**~~ — **resolved by Story 1.6 (2026-08-02):** the missing-credentials message now prints the resolved Key Config path, since "add the key to your Key Config" is only actionable if the user knows which file that is. (Still not echoed on the success path — a cosmetic gap, deliberately left.)

## Deferred from: story-1.6 implementation (2026-08-02)

- ~~**The provider catalog admits providers the v1 runtime engine cannot actually construct.**~~ — **resolved by the Story 1.6 code review (2026-08-02).** CrewAI 1.14.6's native provider list is `openai, anthropic, claude, azure, azure_openai, google, gemini, bedrock, aws, openrouter, deepseek, ollama, ollama_chat, hosted_vllm, cerebras, dashscope` — it has **no** native `groq` or `xai`, and this repo does not install the `litellm` fallback. So an agent routed to `xai` **with a valid XAI_API_KEY present** used to pass the pre-run gate and then fail at `LLM(...)` construction with an `ImportError` reported as a generic "Run failed".

  The original entry got two things wrong, both corrected by the review:
  1. **`google` was omitted, and it was the worst case.** crewai needs the `crewai[google-genai]` extra to call Google natively (`google-generativeai` in the `all` extra is a *different* package), so a `GOOGLE_AI_API_KEY` produced a crash while having *only* an `OPENROUTER_API_KEY` worked — adding the correct key broke the run.
  2. **"`groq` is luckier: it is `openrouter_reachable`, so it works whenever an OpenRouter key is present" was false.** An OpenRouter model id is `<model-creator>/<model>` — the organisation that made the weights, not the company serving them. Groq is an inference *host* and has no vendor namespace, so `openrouter/groq/<model>` is not a valid id; the run was admitted and then rejected by the gateway.

  Fix: a `runtime_supported` flag (plus `unsupported_reason`) on `Provider`, checked by `classify()` via the new `STATUS_UNSUPPORTED_BY_RUNTIME`. `groq`/`xai`/`google` are now refused by the gate before any LLM is constructed, with a message that does *not* ask for a key that would not help; `google`, being a genuine OpenRouter vendor, is pointed at the gateway instead. `groq` had `openrouter_reachable` removed. Re-opening any of these is a one-line catalog change once the engine can reach them.

## Deferred from: code review of story-1.6 (2026-08-02)

- **`ProviderRouting.api_key_env` is dead data** — it is written into every Team Package by the Factory and then never read by `resolve_credential`, which keys off `provider.name` against the catalog. A package built with `api_key_env="GEMINI_API_KEY"` for `google` resolves against `key_config.keys["google"]` and, on failure, tells the user "add GOOGLE_AI_API_KEY to your Key Config" — contradicting the package in their hand. Pre-existing: the Story 1.5 engine also keyed off `provider` rather than `api_key_env`. Note `tests/unit/runtime/test_preflight.py`'s local `_agent` helper overrides the shared factory default to `api_key_env=None`, which is why no preflight test surfaces the discrepancy. Candidate fix: either honor `api_key_env` as a per-package override of the catalog's `env_var`, or drop the field from the package format.
- **`pytest.importorskip("crewai")` makes the AD-7 conformance gate silently skippable** — the file that AD-7 designates as the blocker for every CrewAI version change reports `SKIPPED` and goes green in any environment installed without the `runtime`/`all`/`dev` extra (a plain `pip install team_maker`, a lightweight CI lane, a minimal contributor venv). Deferred rather than fixed because Story 1.6 Task 5 explicitly mandated `importorskip` (matching `test_crewai_execution_engine.py`); closing it properly needs a required-marker mechanism or a dedicated CI lane that fails if the conformance tests do not actually execute.
- **A direct `openrouter` routing entry accepts an unqualified model string** — `provider: openrouter, model: gpt-4o` resolves to `openrouter/gpt-4o`, missing the `<vendor>/` segment that the `via-openrouter` fallback path constructs correctly for everyone else. crewai's openrouter matcher accepts any model string unconditionally, so the LLM constructs, the gate reports available, and the failure only appears as a 400/404 from the gateway mid-run. Nothing validates the `vendor/model` form on the direct path.
- **A zero-agent Team Package passes the pre-run gate** — `check_credentials` loops over `team.agents` with no emptiness check, returns `{}`, and raises nothing; the engine then builds `Crew(agents=[], tasks=[])` and crewai's pydantic validation error surfaces through the CLI's generic handler as `Run failed: <validation dump>`. Pre-existing gap in `runtime/loader.py`, which has no minimum-agent validation.
- **`UnresolvedProvider` with an empty `roles` list renders a dangling `(agents: )`** — `_render_message` does `", ".join(item.roles)` unguarded. Only reachable by constructing `UnresolvedProvider` directly rather than via `check_credentials`, which is exactly what the Epic 5 API path and the new CLI tests do.
- **The conformance env-poisoning test bypasses `KeyConfig.from_file`** — Story 1.6 Task 5 specified asserting against a Key Config *file* carrying different values from the poisoned environment, exercising `from_file`'s documented file-wins-over-env precedence. The implementation constructs `KeyConfig(keys={...})` directly and asserts a narrower engine-only variant, defended in an in-code comment. The AD-7 invariant itself holds — the resolution/preflight/executor/engine chain has zero `os.environ` access — but the file-priority path the task named is covered only by `tests/unit/test_keyconfig.py`, not by the conformance gate.

## Deferred from: story-1.7 implementation (2026-08-02)

- **Partial transcripts on a failed run are not returned.** Story 1.7's AC is scoped to "**When** it completes" — if `kickoff` raises, the recorder's context manager unregisters cleanly but the entries collected so far are discarded along with the exception. PRD Open Q5 ("what happens when a Provider errors mid-run; are partial results returned?") is still open (`prd.md:457-460`), and `EXPERIENCE.md:91` explicitly defers partial-results behavior to it. A partial transcript is arguably *more* valuable than a complete one — it is what tells the user where the run died. Closing this means deciding whether `MissingCredentialsError`-style structured failure should carry a transcript, which is a product decision, not a routing one.
- **Concurrent runs in one process corrupt each other's transcripts.** The crewai event bus is a process-global singleton and its handlers run on a shared worker pool, so two simultaneous `run_team_package` calls each record the other's events. **Measured during the Story 1.7 code review** (two threads held inside `kickoff` by a barrier): run A came back with every entry duplicated, run B *lost* an entry and gained a `task='unknown'`, and the two runs' `emission_sequence` values collided outright — the counter is ContextVar-scoped and restarts at 1 per run, so sorting cannot separate them. `__exit__`'s `flush()` also waits on all process-wide pending futures, so each run blocks on the other's handlers. v1 is single-run-at-a-time (the CLI cannot do otherwise), but Story 5.2's HTTP endpoint is exactly where this breaks. Fix candidates: filter captured events by the run's own task/agent id set, or serialize runs behind a lock in the API layer.
- **The generated `crewai_runner.py.j2` template captures no transcript.** FR-27 is satisfied for the in-process Runtime only; a user running the standalone `run_example.py` from a Team Package still sees only the final answer. Out of scope here for the same reason as Stories 1.5 and 1.6 — it is a codegen change (AD-1), and that template also still carries the known hierarchical-Crew bug (`deferred-work.md:70`) and the AD-7 global-env gap. Whoever fixes one should fix all three together.
- **The transcript conformance test inherits the `importorskip` hole.** `tests/conformance/test_transcript_conformance.py` skips silently where crewai is absent, exactly as the AD-7 gate does (already logged above). This is now a second instance of the same gap rather than a new one; closing it wants a required-marker or a dedicated CI lane that fails when conformance does not actually execute.

## Deferred from: code review of story-1.7 (2026-08-02)

- **ANSI/OSC escape sequences in transcript content reach the terminal verbatim.** `rich.markup.escape` neutralizes `[` only; it does nothing to `ESC`. An agent echoing colourised tool output — or a prompt-injection payload — can move the cursor, clear the screen, or emit an OSC-8 hyperlink when a user runs `team-maker run --transcript`. Confirmed by CLI probe. The written file is unaffected. Not fixed here because stripping control characters is a display-sanitization convention the codebase does not yet have (`deferred-work.md:45` already notes the absence of a scrubbing convention), and getting it wrong would mangle legitimate content.
- **Six deep crewai internal import paths are now load-bearing for running a team at all.** `crewai_execution_engine.py` imports `transcript_capture` at module scope, which imports from `crewai.events.types.*` and `crewai.utilities.string_utils` — non-public paths (`AgentLogsExecutionEvent` is a *logging* event; `sanitize_tool_name` is a utility helper). If any move in a crewai upgrade, the engine module fails to import and `run` cannot execute — an observability regression becomes total loss of the feature. Candidate fix: import the event classes lazily inside `_subscribe` behind a `try/except ImportError` that degrades to an empty transcript, so the run still works.
- **A renamed or removed `emission_sequence` would silently discard the entire transcript.** `_add` does `getattr(event, "emission_sequence", None)` and returns early on `None`, which cannot distinguish "this event has no sequence" from "crewai renamed the attribute". After such an upgrade every entry is dropped, and the CLI reassuringly prints "No transcript was captured for this run." The conformance test would catch it, but only where crewai is installed (see the `importorskip` hole above).
- **`_format_transcript`'s line format is ambiguous and unbounded.** A content line reading `    [7] design / architect (agent_message)` — trivially produced by an agent quoting a prior transcript, which is common in multi-agent runs — is indistinguishable from a real header to any grep or parser, and there is no length cap on a transcript that captures every turn. If the transcript ever needs to be machine-read (Story 5.2), it wants a structured serialization rather than this human format.
- **`TranscriptEntry` carries no run identity.** A streaming or HTTP consumer serving concurrent runs needs to know which run an entry belongs to. Adding a field later is, by AD-13/AC-7's own wording, "a change to the entry type" — the thing the seam was designed to avoid. Worth settling before Story 5.2 builds on the current shape rather than after.
- **Widening `RunResult` changed its generated `__eq__`, `__repr__` and `asdict` output.** Two results that compared equal before (same final output, same per-task results) now differ if their transcripts differ, and `repr(result)` — in a log line or a pytest assertion message — now dumps the entire agent conversation. No existing consumer depends on either today; noted so the next one is not surprised.

## Deferred from: story-2.1 implementation (2026-08-02)

- **The installed shadcn CLI (v4.16.1) is a materially newer architecture than the story's research assumed**, verified by reading the generated output rather than the docs page (per Epic 1 review lesson: verify the real API before writing assertions). Concretely: it scaffolds on **Base UI (`@base-ui/react`)**, not Radix — `SidebarMenuButton`/`Button`/etc. take a `render` prop (`render={<Link href="/" />}`), not Radix's `asChild`; the style is named `base-nova`, not `new-york`/`default`; `globals.css` imports a package (`@import "shadcn/tailwind.css"`) rather than being fully self-contained; and the `@theme inline` block derives `--radius-sm/md/lg` with a **multiplier** (`calc(var(--radius) * 0.6/0.8/1.0)`), not the subtractive formula (`calc(var(--radius) - 4px)` etc.) the story's Dev Notes cited. Story 2.1 overrode the radius formula explicitly in `globals.css` to hit the exact 4/6/8px AC 8 requires. Whoever writes Story 2.2+ should read the actual installed `components/ui/*.tsx` before assuming Radix-era shadcn patterns (`asChild`, `forwardRef`) still apply — they don't.
- **`npm audit` reports 3 high-severity advisories (PostCSS XSS/path-traversal, sharp/libvips CVEs), both transitive through `next@16.2.12`'s own bundled dependencies.** `npm audit fix --force` would downgrade to `next@9.3.3`, which violates AC 1's pinned `^16.2` floor — not actionable without either an upstream Next.js patch or accepting the downgrade. Left as-is; re-check on the next Next.js patch release.
- **No `web/` entry in the Python-shaped `clean` Makefile target** — `make clean` only removes `dist/`, `build/`, `*.egg-info`, etc. Deliberately not added: the story's own instructions warn that a careless addition there would `rm -rf node_modules` on every `make clean`. A future story should add a dedicated `web-clean` target instead of extending `clean`.
- **No CI lane runs `npm test`** — the Python suite has no CI lane either (per the Epic 1 precedent already noted in this file), so the frontend inherits the existing gap rather than creating a new one.
- **AC 10's light-mode primary (`#FFFFFF` on `#0E8C82`, measured 4.12:1) fails the WCAG 2.2 AA 4.5:1 text floor**, though it clears the 3:1 non-text floor the AC actually requires. Shipped as specified per the story's explicit instruction; three remediation options are recorded as Open Question 1 in the story file (darken to `#0D857B` at 4.51:1; restrict primary-button labels to WCAG large text; or accept the deviation). Still open at merge — a product/design decision, not this story's to make unilaterally.

## Deferred from: code review of story-2.1 (2026-08-02)

- **The mobile `Sheet` branch of the sidebar is unreachable in every test.** `web/vitest.setup.ts:5-16` installs a global `window.matchMedia` stub that returns `matches: false` for every query, and jsdom pins `window.innerWidth` at 1024 — so the vendored `useIsMobile()` returns `false` in 100% of tests and `Sidebar`'s `isMobile` branch (`Sheet`, `SheetTitle`, `openMobile`, `setOpenMobile`) never executes. The breakpoint itself is correct — verified live that 767px renders the mobile Sheet and 768px the desktop rail, so `useIsMobile`'s `< 768` and Tailwind's `md:` agree exactly — the gap is test reachability, not behaviour. Closing it needs a per-test `matchMedia`/`innerWidth` strategy (a controllable stub, or a viewport-driven harness) rather than the current always-false global, which is why it is deferred rather than patched inside the review. Note this also means any future component depending on a media query being *true* will silently read `false` in tests.

## Deferred from: story-2.0 implementation (2026-08-03)

- **Compose sessions do not survive anything.** The registry is a plain in-process `dict` (`api/sessions.py`), so a conversation is lost on every process restart — including each `--reload` restart during development, which happens on every file save. It is also why the API is **single-worker**: a second uvicorn worker gets its own empty registry and roughly half of all requests answer `session_not_found` for a session that is very much alive in the other process. `make api-serve` pins `--workers 1` and the app logs a startup warning if `WEB_CONCURRENCY` disagrees, but nothing *enforces* it. AD-11 defers persistence deliberately, and AD-3 says "one deployable process by default", so this is a known v1 boundary rather than a bug — but the moment anything wants two workers, a shared session store has to land first. PRD Open Question 3 (a stable Team reference) is the natural place to settle what, if anything, outlives a process.
- **`model_substitutions` is derived by re-deriving, not by observing.** `normalize_team_routings` mutates the team in place and reports substitutions only to **stderr** (`model_resolver.py:156-185`), so `api/build.py` reconstructs the *requested* routing by running the same public template the pipeline runs (a pure in-memory transform) and diffing it against the built team. That is accurate for the template path and touches nothing under `team_maker/`, but it has two gaps: it reports `[]` for the **planner path** (empty `desired_roles`, where the team is invented by an LLM and there is no client-requested model to compare against), and it silently degrades to `[]` if the pre-resolution ever raises. The clean fix is for `normalize_team_routings` to *return* its substitution list instead of printing it — a small, obviously-correct change to `team_maker/`, deliberately not made here because AC 12 froze every existing module under `team_maker/`. Whoever unfreezes it should also make the API stop re-deriving.
- **The build path can still trigger a silent second LLM call.** An empty `desired_roles` flips `PipelineRunner` into the planner strategy, which calls a *different* provider config (`planning_llm`) and costs money with no signal (`runner.py:66-69`, `llm/planner.py:24-46`). `PUT /api/compose/sessions/{id}/spec` rejects an empty roles list, and the Composer's system prompt requires at least one role, so the reachable paths are covered — but `POST .../build` itself does not guard, because Story 2.0's Task 3 scoped the guard to the edit route only. A spec that reaches the session with no roles by some future path would build at planner cost, silently.
- **`normalize_team_routings` ignores an explicit key file.** It is called with no config (`runner.py:144-145`) so it re-reads the *default* Key Config path. Harmless today because the API resolves one Key Config at startup from the same default path, but it becomes wrong the moment a request can name a key file.
- **A keyless provider's failures are all classified as unreachability.** `api/routers/compose.py` maps any non-`ComposerError` exception from a `keyless_local` provider (today only `ollama`) to `authoring_unavailable`, on the reasoning that a local, credential-free provider essentially only fails because nothing is listening. It is a data-driven branch on the catalog row rather than on provider name, and it is what AC 10's "a keyless provider can only produce this error for a connection failure" asks for — but it does mean a genuine bug inside a keyless adapter would be reported to the user as "start your local provider". Distinguishing them properly means recognising SDK-specific connection-error types in `api/`, which would put provider knowledge back into the layer AD-8 exists to keep it out of.
- **No CI lane runs `pytest` or `npm test`.** Inherited, not new — Epic 1 logged the Python side and Story 2.1 logged the frontend side. Story 2.0 adds a third suite (`tests/api/`, 89 tests) with nothing automated watching it, and two of its guards (the AC 3 concurrency proof and the AC 4 credential sweep) are exactly the kind that rot silently when only run by hand.
- **The CLI is still Anthropic-only.** `api/` now selects its authoring provider parametrically (AC 10), but `team_maker/cli.py:37-38` remains hardcoded to `anthropic`, and `compose --provider` does not exist. The asymmetry is deliberate — `cli.py` was outside Story 2.0's scope — but it means the same user gets a working Composer in the browser and a dead one in the terminal. The port already exists; this is roughly a `--provider` option plus reusing the same catalog-row lookup `api/deps.py` now does.
- **`api/deps.py` duplicates ~10 lines of `cli.py`'s credential logic on purpose.** `_resolve_authoring_provider` and `_bridged_credential` are private members of the CLI layer; importing them from `api/` would invert AD-4 and drag `click`, `rich` and `PipelineRunner`'s import chain into the API process. The duplication is small and the two are diverging anyway (the API's is parametric and bridges once at startup rather than per call). Promoting a shared credential module under `team_maker/` is the eventual fix and would let the CLI's `--provider` land at the same time.

## Deferred from: code review of story-2.0 (2026-08-03)

- **`fields[].message` carries pydantic-derived text.** `api/errors.py`'s module docstring states as an absolute that *"`message` is authored copy, never `str(exc)`"*. That holds for `error.message` at every raise site, but `error.fields[].message` is the `msg` half of a pydantic or `ComposerError` string forwarded verbatim (`fields_from_composer_errors:116-137`, `fields_from_error_list:140-160`). Pydantic's custom-validator messages routinely interpolate the offending input, and here that input is LLM output derived from the user's intent. AC 8's literal requirement — no traceback, no exception class name — is met, and `assert_no_exception_leak` enforces exactly that, so nothing leaks today. But the guard has no notion of "was this text authored", and the prose above it claims a property one of its two fields does not have.
- **`context_dir` breaks the spec round-trip if the directory disappears.** `PUT .../spec` reconstructs the spec with `TeamCreationRequest(**current.model_dump())`, and `validate_context_dir` (`request.py:374-382`) requires an *existing* directory. If the composer set `context_dir` and it is later removed or unmounted, an unrelated edit such as `{"team_name": "X"}` fails 422 blaming a field the client never sent. Verified. Every other field round-trips cleanly, so this is the single asymmetry — it exists because one validator consults the filesystem.
- **The OpenRouter adapter inherits three robustness holes from its template.** `response.choices[0]` is unguarded (`:83`), and OpenRouter specifically returns an `error` member with an empty `choices` list for upstream model errors, no-credit accounts and moderation blocks — an `IndexError` rather than a diagnosable message. The fallback `json.loads(match.group())` (`:96`) re-raises a bare `JSONDecodeError` instead of the authored `ValueError` the sibling branch produces. And the `OpenAI` client (with its connection pool) is constructed per call and never closed — invisible in a CLI, accumulating in a long-lived uvicorn process. All three are character-identical to `xai_provider.py`, which is what AC 11 required; fixing one adapter and not the other reintroduces the two-patterns problem that AC explicitly guards against. The honest fix is to fix both, which touches an existing module under `team_maker/`.
- **`_guarded` still cannot tell our bugs from the provider's — but it no longer claims to.** The bare `except Exception` in `api/routers/compose.py` maps everything that is not a `ComposerError` to `compose_failed` (502), so a `TypeError`, `AttributeError` or `KeyError` from a genuine defect in `team_maker/` or in the mapping layer lands in the same branch as a network fault. **Partly resolved at review:** the copy was causally neutral-ised — it used to read *"The authoring provider could not be reached. Please try again."*, which blamed the upstream for our own defects and invited a retry loop that spends money and can never succeed, and now reads *"The team specification could not be created. Retry once; if the problem repeats, stop and report it."* Containment was always correct and is regression-tested end to end (`test_an_internal_typeerror_during_refinement_is_contained_and_neutral`). **What remains deferred is the classification itself**: distinguishing a connection failure from an internal defect means recognising SDK-specific exception types (`APIConnectionError`, `APITimeoutError`, and their vendor equivalents) inside `api/`, which puts provider knowledge back into the layer AD-8 exists to keep it out of. Deliberately not attempted in Story 2.0. The clean shape is probably a small classification helper in the adapter layer — where provider knowledge already lives — that maps an SDK exception onto a port-level category the API can branch on as data. That would also let the keyless-`ollama` trade-off logged above be retired at the same time, since both are the same missing abstraction.
- **`include = ["api*"]` claims a generic top-level package name.** The edit is required by AC 5 and is correct for the editable install, but it also ships a top-level `api` package into site-packages in any wheel. That is an unusually generic name to claim and will collide with any other distribution doing the same. Nothing breaks today because the project is only ever installed editable, but a future `pip install team_maker` alongside another package exporting `api` is a real conflict. Worth settling alongside the packaging question in the story's Open Question 5.
- **`fields_from_composer_errors` assumes a `loc` never contains a colon.** It splits on the first `": "` (`api/errors.py:130`) and the inline comment asserts the loc segment is only joined role names and list indices. `sandbox.extra_env` is a `Dict[str, str]` (`request.py:127`) whose keys are arbitrary strings, so a validation failure under a key containing `": "` yields a wrong, unaddressable field path. Low reachability — it needs the authoring LLM to emit a colon-bearing dict key that also fails validation — but the stated invariant is not true, and Story 2.2's inline validation reasons depend on these paths.
- **Reading a spec mutates it.** AC 2 authorises no `GET` session route, so the only way to read the stored spec back is `PUT .../spec {}`, which runs the full merge-and-reconstruct path and replaces `session.current` with a re-validated object. Verified idempotent for every payload the suite exercises, and the story's own tests use it this way — but it is a write pretending to be a read, and Story 2.2's review mode should not lean on it without either a real read route (an AC 2 contract change) or an explicit note that the round-trip is load-bearing.
- **The build path still does not guard empty `desired_roles`.** Already logged above under the story-2.0 implementation entry; the code review confirmed it independently and adds that `PUT .../spec` conversely *rejects* an untouched planner-path spec with a 422 blaming `desired_roles` — a field the client never sent. The two guards are inconsistent with each other: the edit route treats an empty roles list as invalid, while the build route treats it as a supported planner path.

## Deferred from: story-2.2 implementation (2026-08-03)

### Contract friction found against Story 2.0's endpoints

- **`output_exists` tells the user to do the one thing the UI is forbidden to offer.** The 409 copy reads *"Choose a different output path, or remove the existing directory."* (`api/build.py:31-34`), but `output_path` is server-owned and read-only to the browser (2.0 AC 13), so the Composer has no "choose a different path" affordance and is explicitly barred from adding one. The remaining advice — remove the directory — is an instruction to go and use a file manager on the server's filesystem. The message is rendered verbatim because it is authored server copy and Story 2.3 owns error-copy refinement, but it currently asks for an action the surface cannot support. Captured live: `web/tests/composer/fixtures/error-output-exists.json`. The natural fixes are either a server-side de-duplicating path (e.g. `article_team-2`), or an explicit overwrite affordance, neither of which this story may add.
- **A second build in the same session can only ever fail.** `output_path` is derived once from the first spec and pinned for the session's life, so once a build succeeds, every subsequent build of that conversation returns `output_exists` — including after a refinement that legitimately changed the team. The user's only route to a second package is to start a new conversation, which also throws away the transcript. Reachable in one click and confirmed live (two `POST .../build` calls, 200 then 409).
- **There is still no read route for the spec.** Already logged under the 2.0 review as "reading a spec mutates it". Story 2.2 does **not** lean on `PUT .../spec {}` as a read — the editor seeds from the spec already returned by the last create/message/save — so the load-bearing round-trip that entry warned about was avoided. Recorded here only to confirm the warning was heeded.
- **`compose_failed` could not be provoked at all.** A deliberately bogus authoring model (`{"provider":"anthropic","model":"claude-not-a-real-model"}`) returned **201** with a perfectly good spec rather than faulting, so the adapter evidently does not validate or reject the model id it is handed. That means an authoring model typo is silently ignored and the request is served by some other model, with nothing reported to the user. Worth a look on the adapter side; it also left `compose_failed` as one of four codes with no captured fixture.

### Left open by this story

- **`turn_cap_reached`, `compose_failed`, `build_failed` and `session_busy` have no captured fixture.** They need 20 real LLM turns, an unprovokable fault, an induced internal error and a race respectively. Their tests synthesise the envelope from the server's own copy strings and say so out loud (`web/tests/composer/fixtures/index.ts`, `error-paths.test.tsx`). If any of those four messages is reworded server-side, the frontend tests will keep passing against stale copy — a real, declared gap that a shared contract fixture (or a server-generated error catalogue) would close.
- **The transcript does not survive a reload, by design and without any warning to the user.** The API keeps no chat history, so the conversation exists only in React state. A refresh loses it silently. `sessionStorage` would fix the common case, was not specified anywhere, and is deliberately not invented here.
- **Task dependencies are displayed but not editable.** The editor shows "Runs after X" and sends the list back unchanged. A client-side check rejects a dependency naming no task (which the server does *not* check — the template prunes dangling dependencies in silence), but re-ordering the DAG is not possible from the UI.
- **No provider/model picker for the authoring turn.** `authoring` is omitted on every request, so the server default is always used. The story explicitly does not require the picker, and `authoring_unavailable` already names the provider and Key Config entry when the default is unusable.
- **No browser test lane.** `web/tests/composer/e2e-live-check.mjs` is a manual harness — real Chromium against real servers, run by hand, not collected by vitest and depending on a `playwright` install that is deliberately not a project dependency. It passed at implementation time (see the story's Completion Notes) but nothing runs it automatically, so it will rot exactly like the guards the 2.0 review flagged. Wiring a real E2E lane is a Test Architect task (`bmad-testarch-framework`), not a Composer one.
- **`components/ui/popover.tsx` is installed and unused.** Task 1 required installing it; the editor needs no second layer because the provider control is a native `<select>`, which caps modal depth at one more simply than a correctly-nested Popover would. Left in place rather than deleted, since it is vendored, regenerable output and Story 2.4's badge picker is its likely first consumer.
- **Autoscroll is unconditional.** The transcript scrolls to the end on every append, with no scroll-anchoring and no "you have scrolled up" suppression, so a user reading back up is yanked to the bottom when a turn lands. Specified nowhere; the simple behaviour was chosen deliberately and Story 2.4 inherits it.
- **Story 2.1's light-mode primary contrast (4.12:1) is now user-visible.** It is on the `Run it now` and `Build team` labels, which are normal-size text below AA's 4.5:1. Not changed unilaterally — see Open Question 1 in the story file, still unresolved and now shipping on a real surface rather than a placeholder.

## Deferred from: code review of story-2.2 (2026-08-03)

- **A second build in any session can only ever return 409.** `api/output.py` derives `output_path` once, from the *first* spec's `team_name`, and pins it for the session's life; `api/build.py` treats an existing directory as a hard `output_exists`. So the moment a build succeeds, that conversation can never build again — including after a refinement that legitimately changed the team, which is the whole point of AC 1. Restarting does not help: the same description yields the same slug and the same 409. Already logged under the story-2.2 implementation entry as contract friction; the code review confirmed it independently and adds two things. First, the client makes it worse: the doomed second attempt clears `state.build`, wiping the success panel that is the only place `output_path` is ever shown, and no `Start a new conversation` affordance appears because that is gated on `expired`. Second, the test that exercises a second build stubs a **200** — a response the real server cannot produce — so the mock is precisely what hides the dead end. The client-side half is patchable in this story; the server-side pinning is Story 2.0's surface.
- **The `g` chord fires from focus on any Composer button and destroys the conversation.** `nav-shortcuts.tsx`'s `isTypingTarget` exempts `INPUT`/`TEXTAREA`/`SELECT` and three `contenteditable` values — not `BUTTON`. So after clicking `Send`, `Build team`, `Run it now`, `Dismiss` or the review Switch, typing `g` then `t` navigates to `/my-teams`, unmounting `ComposerSurface` and taking the transcript, the spec and the record of any build with it. The server session survives but is unaddressable: there is no session-list endpoint. Story 2.2's AC 6 required only that the chord not fire while typing in the composer, which is implemented and tested, so this is outside what the story was asked to do — but the app has no unsaved-work guard anywhere, and this surface is the first one where navigating away destroys something the user cannot recover. Worth settling before Story 2.4 adds a workspace with even more transient state.
- **Three test files exceed CLAUDE.md's ~400-line guideline**: `web/tests/composer/api-client.test.ts` (450), `build.test.tsx` (412), `error-paths.test.tsx` (401). Every authored *source* file is inside the range (largest `spec-editor.tsx` at 375). Noted because the story split a 649-line suite for exactly this reason, so the rule was applied unevenly rather than consistently — the natural next cut is by concern (transport vs parsing in `api-client.test.ts`, editor vs outcome in `build.test.tsx`).
- **The review editor cannot add or remove a role or a task.** Deferred by decision during the Story 2.2 code review (2026-08-04): adding roles and tasks is net-new authoring surface rather than the *editing* AC 4 asks for, and the conversation is the intended way to change the team's shape. The consequence to carry forward is that the empty-roles refusal Task 4 explicitly required — in `spec-draft.ts`'s `draftIssues` and again in `api-client.ts`'s `validateEdit` — is **unreachable through the UI**, because no role can be removed. Both are unit-tested and neither is exercised end to end, so if the guard ever regresses nothing user-facing will notice. Whoever adds add/remove controls should make reaching that guard part of the same change.

## Deferred from: story-2.3 implementation (2026-08-04)

### Resolved by this story (recorded so the entries above are not re-litigated)

- **`output_exists` no longer tells the user to do the thing the UI is forbidden to offer.** The story-2.2 entry above is fixed at the source: `api/build.py`'s copy now reads *"A directory already exists where this team would be written, so the build stopped rather than overwrite it. The destination is chosen by the server from the team's name; build a differently-named team, or remove the existing directory."* The fixture was re-captured against a live server (`web/tests/composer/fixtures/error-output-exists.json`), and Story 2.2's client-side `OVERRIDDEN_SERVER_COPY` override was **removed** rather than left masking a defect that no longer exists. The underlying friction — *a second build in any session can only ever 409* — is untouched and remains Story 2.0's surface.

### Left open by this story

- **The key gate is enforced in the UI only, and is bypassable.** UX-DR5 says a missing key "blocks the run", but there is no run endpoint — `epics.md:335` assigns the `api/` run group to Story 2.4 — so what this story blocks is the **build**, in the browser. `GET /api/keys/check/{id}` reports `blocked: true` and the Composer refuses all four build entry points, but `POST .../build` itself does not consult the check, and `PipelineRunner.run` has no credential gate of its own (Story 1.6's gate covers `team-maker run` only). Any client that is not this UI can still build a team it cannot run. **Story 2.4 should enforce it server-side when it adds the run endpoint**, which is also the point at which FR-10's "fail fast at run start" becomes literally true over HTTP.
- **A key added while the API is running is usable for a run but not for composing.** Measured, not assumed. The authoring adapters read `os.environ.get(api_key_env)` and `api/deps.py:bridge_credentials` publishes credentials once during lifespan startup — deliberately, because a per-request environment mutation races (`api/deps.py:12-24`). Team runs resolve credentials from the Key Config directly (`adapters/providers/resolution.py`), and `normalize_team_routings` re-reads it (`model_resolver.py:170`), so both pick a new key up immediately. Composing does not. Rather than report a status that is true for one path and false for the other, the endpoints expose `needs_restart_to_author: [provider…]` and the UI says so in plain language. The clean fix is to make the authoring adapters take a credential rather than read the environment, which is an `LLMProvider` port change and well outside this story.
- **`xai` carries `openrouter_slug="x-ai"` but leaves `openrouter_reachable` at its `False` default** (`registry.py:97-103`), so the slug is dead data and an OpenRouter key does *not* make `xai` usable — it reports `unsupported-by-runtime` even with a gateway key present. This is the same defect class as `ProviderRouting.api_key_env` being dead data (logged above): a field that exists, looks load-bearing, and is never read. Either the row should be `openrouter_reachable=True` (x-ai is a genuine model-creator namespace on OpenRouter, unlike groq) or the slug should go. Not changed here — this story only projects `classify()`, and altering the catalog would change what a *run* resolves, which is not its surface.
- **The provider-level read reports only `no-keys` / `has-keys`, never a four-state verdict.** `all-good` and `missing-key` are judgements about a specific team's roles, and `GET /api/keys/status` has no team; the catalog permanently contains a provider the runtime cannot use (`groq`) and one needing no key (`ollama`), so any whole-catalog aggregate is meaningless. Story 2.6's Settings surface will want the per-provider list this route already returns, not an aggregate.
- **The no-keys banner's spine copy overstates the requirement.** `EXPERIENCE.md:87` says *"You'll need at least one model key to run"*, but `EXPERIENCE.md:129` says local models run keyless and `ollama` is unconditionally usable, so a pure-ollama setup can run with an empty Key Config. Spine copy ships verbatim; the contradiction is a question for the PM (see the story's Open Questions).
- **`EXPERIENCE.md:87`'s "Links to Settings guidance" is deliberately not built.** Settings holds no key guidance until Story 2.6 and `EXPERIENCE.md:104`/`:172-174` ban dead affordances, so the banner names the Key Config **path** instead — truthful and actionable today (the Story 1.6 precedent). Story 2.6 should add the link when there is something to land on.
- **`fields[].message` is still not authored copy.** Logged above under the 2.0 review and deliberately not fixed here: it forwards pydantic/`ComposerError` text while `api/errors.py:5-8` claims otherwise. Fixing it needs a pydantic-message→copy catalogue, and the client already scrubs leaks structurally (`looksLikeLeakedInternals` + `scrubFields`). Story 2.3 owns error *copy* and is declaring this one out of scope rather than leaving it unmentioned.
- **The key check re-reads the Key Config on every request.** Two small file reads per Composer render cycle. Correct — a boot-time snapshot would report the pre-edit truth forever, breaking the feature in the flow it exists for — but uncached, and `KeyConfig.from_file()` also walks `PROVIDERS` for the env fallback each time. Fine at one user; worth a short TTL if the API ever serves more.
- **No test covers the `via-openrouter` aggregate against a captured body.** `key-status-no-keys.json` and `key-check-missing-key.json` are deterministic captures, but a `via-openrouter` *check* depends on which keys the capturing machine has, so the frontend test for that state derives it from the captured all-good body and says so in the test. The server side is covered directly (`tests/api/test_key_status.py`).
- **`tests/api/test_review_patches.py` remains at 636 lines**, over CLAUDE.md's guideline. This story added no tests to it — new work went to `tests/api/test_key_status.py` and `tests/api/test_routings.py` — but the file still wants splitting by concern.

## Deferred from: code review of story-2.3 (2026-08-04)

- **`keyNoteFor` joins on role name only, so renaming a role onto another row's name duplicates that row's note.** `web/components/composer/spec-editor.tsx`. Both rows `find` the same `keyRoles` entry, and if the provider matches, both render the same note. The text is not false — status is purely a function of provider — but it is attributed to a row nobody checked, which is the precise thing `keyNoteFor`'s staleness rule exists to prevent. Saving rejects duplicate names anyway (`request.py:407-417`), so the window is short. Fix by matching on index-plus-name, or by suppressing the note when a name is not unique in the draft. Deferred: low impact.
- **`_TEMPLATE_ID = "software_delivery_team"` is hard-coded in `api/routings.py`, and now gates builds as well as reporting.** Pre-existing — `api/build.py` hard-coded the same id before this story — but the stakes rose when the key check started deriving its verdict from it. If the pipeline's template selection ever becomes conditional or client-specified, the key check will green-light providers the actual build never uses and block on ones it does. The clean shape is for the pipeline to expose which template it *would* select for a given request, rather than two call sites guessing. Deferred: pre-existing, and no second template exists yet.

### Left open after the story-2.3 code review (2026-08-04)

- **No way to continue with a subset of a partly-credentialed team.** Decision from Guru during review: if only some members lack valid credentials, the user should be able to proceed with the accepted ones *when the team remains viable*, and any member removal or provider/model reassignment must be **explicit and confirmed**. Reassignment satisfies that today — the review editor changes a role's provider and `Save` re-validates. Removal does not exist: the editor cannot remove a role at all (logged above from 2.2's review), so there is no silent-drop path and equally no subset-run affordance. Building one is net-new authoring surface plus a viability rule ("which roles are load-bearing?") that nothing in the spec defines, so it is recorded rather than invented. Whoever adds add/remove controls owns this at the same time.
- **Better key guidance at Settings, so "(Settings)" becomes actionable.** `EXPERIENCE.md:86`'s missing-key sentence is now shipped verbatim, parenthetical included, per Guru's decision to follow AC 4 rather than add a second deviation. Settings currently holds nothing key-related (Story 2.6 owns it), so the pointer is aspirational: the Key Config **path** is rendered beside it as the actionable locator. Story 2.6 should add the per-provider status list, the Key Config path, and safe-key guidance, at which point the parenthetical stops needing the path next to it.
- **Four `web/` files are over CLAUDE.md's ~200–400 guideline**: `lib/api-types.ts` 560, `components/composer/spec-editor.tsx` 517, `lib/api-client.ts` 454, `components/composer/composer-surface.tsx` 428. `tests/api/test_key_status.py` reached 728 during this story and *was* split (into `test_key_status.py` 349 + `test_key_check.py` 376 + `keyroutes.py`); the four above were not, because each splits along a different seam worth thinking about rather than guessing at — `api-types.ts` by domain (compose vs keys), `spec-editor.tsx` by row type, `api-client.ts` transport vs routes, `composer-surface.tsx` by extracting the two key effects into a hook.
- **The key routes re-read the Key Config twice per request** (once with the environment fallback, once without) to attribute the credential source. Four small file reads per Composer render cycle. Correct and cheap at one user; worth a short TTL if the API ever serves more.

## Deferred from: story-2.4 implementation (2026-08-09)

### Resolved by this story (recorded so the entries above are not re-litigated)

- **The dangling `(agents: )` for a role-less `UnresolvedProvider` (logged under story-2.3 implementation) is fixed and verified, not merely inherited.** `preflight.py:242` renders `"none recorded"` for an empty `roles` tuple. Checked directly against the source before writing this line rather than trusted from the entry's own age.
- **The key gate is enforced server-side for the *run* path** (logged under story-2.3 implementation as fully open). `POST /api/runs` now runs `load_team_package` → `executor.check_runnable` → `preflight.check_credentials` synchronously, before the run's background thread is spawned, and refuses with `run_blocked` (409) naming the real cause. **Only the run gate is closed.** `POST .../build` still does not consult it — see below, carried forward rather than widened into here.
- **`deferred-work.md:112` (`TranscriptEntry` carries no run identity) is resolved the way that entry hoped for.** `run_id` lives in the `api/` run record (`RunRecord.run_id`); `TranscriptEntry` itself gained no field. Story 5.2, whenever it arrives, inherits this shape unchanged.

### Left open by this story

- **`POST .../build` still does not consult the key check, and `PipelineRunner.run` has no credential gate of its own.** Carried forward verbatim from story-2.3: any client that is not the Composer can still *build* a team it cannot run — only *running* it is now gated. Story 2.0 owns that route; widening this story's scope to cover it was explicitly declined (AC 7's own text).
- **Concurrency is one run per process, not fixed.** `api/runs.py`'s `RunRegistry` serialises runs behind a process-wide lock, which closes all three of the causes measured in the story-1.7 review (handler fan-out, `emission_sequence` collision, cross-run `flush()` blocking) — but two runs still cannot execute concurrently in one process, ever. A multi-worker or multi-run deployment needs a structurally different fix (e.g. per-run event filtering keyed on a request-scoped identifier crewai does not currently expose, or a queue), not attempted here.
- **A failed run's transcript is discarded, now reachable over HTTP for the first time.** `deferred-work.md:101`'s "the entries collected before `kickoff` raised are discarded with the exception" was previously CLI-only; the Workspace's transcript dialog now renders the same "nothing captured" state a browser user can actually hit. PRD Open Q5 (partial results) stays open.
- **Per-task run progress is not shipped.** `RunView` and the transcript are both batch-on-completion (AD-13 v1; PRD Open Q4 stays open). The Workspace shows a truthful task *count* while running and an accent pulse on the run as a whole, never a fabricated "task 2 of 4." The seam for v2: one crewai `TaskStartedEvent`/`TaskCompletedEvent` subscriber inside `transcript_capture.py`, safe now that AC 3 serialises runs (only one run's events exist in the process at a time), exposed as a Runtime-owned callback so `api/` still never imports crewai directly. **Accessibility deviation (Story 2.7 AC 7):** `EXPERIENCE.md:112-113` requires literal "Task 2 of 4, writer, running" `aria-live` progress, which is architecturally impossible in v1. The existing three-state announcement ("Run started. N tasks.", "Run complete.", "Run failed. {reason}") is kept as the AA-compliant v1 floor, with this deviation formally documented rather than silently shipped short of spec.
- **The team reference is a slug, still provisional.** `GET /api/runs/teams/{slug}` and `POST /api/runs` both resolve a client-supplied value through `slugify_team_name` against `output_root()`. PRD Open Q3 (a durable team reference) is unaddressed by this choice; Story 2.5 was already assigned to settle it and still does.
- **Documents are text-only; PDF/binary attachment remains a possible future story, not started.** `EXPERIENCE.md:188` shows a PDF being dragged in; nothing in this repository parses one. Raised as PM Open Question 2 in the story file rather than decided unilaterally.
- **`"Save this team and its results?"` (`EXPERIENCE.md:90`) is omitted, not built.** Story 2.5 owns the persistence behind it; building the prompt without the persistence would be a dead affordance (`EXPERIENCE.md:103-104` bans exactly that).
- **The run-failed state uses a plain alert, not a `Toast` — `EXPERIENCE.md:91` names one and none is installed.** Same choice Story 2.2 made for build failures; not revisited here. Installing a toast library, if ever done, should probably absorb both surfaces' failure presentation at once rather than adding a second one-off.
- **`components/ui/popover.tsx` remains installed and unused for a second story running.** `deferred-work.md:165` guessed Story 2.4's badge picker would be its first consumer; AC 15 explicitly ruled the picker out of scope, so the guess is wrong twice now. Left in place as vendored, regenerable shadcn output.
- **The `g`-chord unsaved-work gap (`deferred-work.md:172`) is improved, not closed.** A run now lives server-side behind a `run_id`, so navigating away no longer destroys the run itself — only the view of it. Attached documents (never sent until `Run` is pressed) and the on-screen chat log are still lost to a stray `g`+destination chord. Still no global unsaved-work guard anywhere in the app.
- **Story 2.1's light-mode `--primary` contrast (4.12:1, below AA's 4.5:1) is now on a third surface's primary action** (the Workspace's `Run` button, via the same shadcn `Button` default the Composer's `Build team`/`Run it now` already use). Not changed unilaterally; still escalated, same as the story-2.2 entry above.
- ~~**My Teams / Starter Teams UI accessibility baseline (Story 2.7 AC 8):**~~ **My Teams half resolved by Story 2.8 (2026-08-15).** `web/app/my-teams/page.tsx` now fetches and lists real saved teams (browse, reopen, re-run, rename, delete), inheriting this story's floor (headings, landmarks, focus ring, color+label pairing, keyboard operability, Esc-closing dialogs) as its baseline rather than a fresh audit — exactly as this entry anticipated. **Starter Teams remains open**: `web/app/starter-teams/page.tsx` is still the placeholder `EmptyState` stub (Epic 3 has not started); whoever builds its browsing UI still inherits this floor the same way.
- **`web/lib/api-types.ts` and `web/lib/api-client.ts` were split into packages this story** (`web/lib/api-types/{primitives,errors,compose,keys,run}.ts`, `web/lib/api-client/{transport,compose,keys,run}.ts`), unlike every prior oversized-file entry in this log, which was flagged and left. Recorded here as the one file-size entry from this story that is *resolved* rather than deferred, so a future reader does not go looking for these two files at their old single-file paths.

## Deferred from: code review of story-2.4 (2026-08-09)

**Review coverage was partial.** Five of six planned review layers terminated early on an infrastructure fault (API credit exhaustion): the edge-case, acceptance-audit, concurrency, security and frontend layers. Only the context-free adversarial layer completed, and its findings were verified by hand before being recorded. **No acceptance audit against AC 1-16 was performed for this story** — a later reviewer should not read the absence of AC findings here as evidence of AC conformance.

- **The "process-wide" run lock is per-`RunRegistry` instance, not enforced process-wide.** `api/runs.py:122` is an ordinary `threading.Lock()` on an object the lifespan constructs; the module docstring at `:8-19` calls it process-wide, which is true only because `create_app()` is called once in production. `tests/api/conftest.py`'s `make_client` builds a fresh app, and therefore a fresh lock, per test. The invariant is asserted by comment rather than by code — defect class 5, left standing because the production claim does hold.
- **The `pollEpoch` guard in the Workspace reducer is unreachable.** `workspace-state.ts:129` discards a `run_updated` whose epoch is stale, but the polling effect lists `pollEpoch` in its dependency array, so React tears the old effect down (setting `cancelled`) before any render in which the epoch differs. `workspace-state.test.ts` reaches the branch only by hand-dispatching a decremented epoch. Harmless redundancy, but it is a guard that cannot fail (defect class 1) and should not be cited as proof that superseded polls are handled — `cancelled` is what handles them.
- **`test_documents_are_never_written_to_disk` cannot catch the regression it names.** The test injects a `FakeExecutionEngine`, and the real execution engine is the only component in the run path that could plausibly persist document text. The test proves the `api/` layer writes nothing, which is worth proving, but AC 6's claim is broader than what is tested.
- **The Workspace test harness's "loud on unexpected request" property does not hold on this surface.** `web/tests/workspace/harness.tsx:34-40` throws on an unqueued request so a component that stopped calling the API cannot look green. The throw happens inside the stubbed `fetch`, which `transport.ts`'s `request()` converts into an `ApiFailure`, which every Workspace effect then discards via `if (result.ok)`. Flagged unverified: this review did not trace `transport.ts`'s catch to confirm.
- **`logger.exception` on a run failure can write attached document text to the server log.** `api/runs.py:227` logs the exception and traceback; `run_context.augment_team_for_run` splices every document's full text into every task description, which becomes the prompt. A provider error that echoes the offending request (context-length, content-policy, a 400 quoting the body) puts that text in the log — contradicting `api/schemas.py:256`'s "never persisted, never logged". The generic `failure_reason` is safe; the log is not.
- **The lifespan's shutdown branch is not in a `try/finally`, and `_in_flight_thread` is never cleared.** `api/main.py:118-121` calls `run_registry.shutdown()` after `yield` with nothing protecting it, so any exception propagating through the lifespan skips the one line that bounds a run thread outliving the app. Separately, `api/runs.py:210` reads a `_in_flight_thread` that `start()` sets and nothing ever resets, so the registry permanently retains a reference to the last finished `Thread`.
- **The Workspace link does not URL-encode its path segment.** `web/components/composer/build-result.tsx:76` builds ``href={`/teams/${slug}`}`` from a value derived by splitting a server filesystem path, while `web/lib/api-client/keys.ts`'s `sessionPath` documents the opposite rule one directory away ("an id that ever contained a slash would otherwise address a different route entirely"). Bounded today by `slugify_team_name`'s charset, which this code path does not run.
- **`RunView.tasks` is transmitted for a reason nothing consumes.** `api/runs.py:81-88` snapshots the plan at run start specifically so "a run's reported plan cannot change under a client mid-poll", but `workspace-surface.tsx:200-204` renders `TaskList` from `state.plan.tasks` — the mount-time plan — and reads `run.tasks` only for a count in `RunStatus`. If the team is rebuilt between mount and run, the row list and the announced count disagree, and `outputByName` silently drops output for any task the stale list does not know.
- **Attached document text can spoof the run-context delimiters.** `team_maker/runtime/run_context.py:76-80` frames the injected block with `--- This run's goal ---` and `--- Attached document: "<name>" ---`. A document (or goal) containing those exact strings can forge an additional section. Inherent to in-context injection rather than a defect in this implementation, and worth one sentence beside the story's existing acknowledgement that document text can reach the transcript.
- **`test_the_policy_constants_are_named_not_magic` asserts nothing that can fail.** `tests/api/test_run_registry.py:84` checks `RUN_IDLE_TTL_SECONDS > 0` against a literal `30 * 60.0` one file away, and does not assert `MAX_STORED_RUNS` — the constant that actually governs a bound, and the one the ghost-record finding in this review shows can be exceeded.

### Resolved by the code review's fix pass (recorded so the entries above are not re-litigated)

- **A goal an engine cannot honour is now refused, not silently discarded.** `ExecutionEngine.run(team, credentials, goal)` keeps the signature Story 1.7 AC 7 pins, and `CrewAIExecutionEngine.run` still never reads `goal` — the goal reaches the model through the task descriptions. What changed is that the dependency is enforced: `run_context.require_goal_injected(team, goal)` raises `GoalNotInjectedError` before any crewai object is built. It checks the injection mechanism's *own* marker plus the goal text, so there is one goal-propagation path, not two. A blank goal and a task-less team satisfy it vacuously and deliberately — neither has anything an engine could ignore. This closes the `AgentSpec.tools` shaped "field that exists, looks load-bearing, and is never read" that the run path had just added a fifth instance of.
- **The run registry can no longer strand a record in `running`.** `_execute` catches `BaseException` (re-raising anything that is not an `Exception` once recorded, so a `SystemExit` still kills its thread loudly), and a failed `thread.start()` withdraws the record it had already inserted. Both paths previously left `finished_at is None` forever, which both eviction rules skip — so a single escape leaked a record permanently *and* eroded `MAX_STORED_RUNS`, whose own docstring promises the opposite.
- **A run's terminal state can no longer be observed half-published.** `RunRegistry.get` and `start` return `dataclasses.replace` snapshots taken under `_guard`, and `_execute` assigns `status` last. The route read `result` then `status` while the thread wrote `status` then `result`, so `GET /api/runs/{id}` could answer `status="complete"` with `result=null` — and because the client stops polling on a terminal status, that was the final state the user was left with, not a transient blip.
- **The Workspace's per-agent key badges no longer disagree with the key panel.** `api/routers/run.py` passed the same `KeyConfig` in both the `config` and `file_config` slots of `provider_reports`, so `keystatus.credential_source` answered `key-config` for every provider holding a key from any source: the "key found in the environment" note and the whole `startup-leftover` branch were unreachable on that surface. Both routes now read through `deps.current_key_config()` / `deps.file_only_key_config()`, which is also what makes a key added after startup visible to a run without a restart — the behaviour `deps.providers_needing_restart` exists to distinguish.
- **A task dependency cycle is a `run_blocked` refusal, not a 500.** `topological_sort` raises `TaskDependencyCycleError`; the synchronous gate caught only three types and the plan route sorted outside any handler at all. A cyclic package loads cleanly and passes both `check_runnable` and `check_credentials`, so this was reachable on two routes. The copy says no key will fix it.
- **`UnsupportedFrameworkError` carries the framework as a field and the API sanitises it.** `loader.py` reads `primary_framework` verbatim from `team_config.yaml` with no validation, so the previous `str(exc)` put arbitrary on-disk text into a response body under a comment claiming the value was "already constrained to a safe charset by the compose pipeline" — untrue of a package the pipeline never produced. `InvalidPackageError`'s message goes through `safe_label` at sentence length for the same reason.
- **The Workspace stops polling a run whose record has gone.** A poll answering `run_not_found` is permanent, not transient; treating it as retryable left the tab polling every two seconds indefinitely and the `Run` control pinned on "a run is already in progress", escapable only by reloading. `isRunInFlight` now accounts for it and the surface states what happened.
- **Attached documents are keyed by a client-assigned id, bounded at attach time, and only the ones actually sent are cleared.** Keying by filename gave duplicate React keys and made "Remove" delete every same-named document; the count cap was compared against a closure-captured prop, so one multi-file drop bypassed it entirely; the total-characters cap and the empty-file case were checked only at Run time; and `run_started` cleared the whole tray, destroying anything attached while the POST was in flight.
- **A transcript that failed to fetch says so.** The failure was swallowed, and the dialog then showed "No transcript is available for this run yet" — a claim the server never made about a transcript that exists. Reopening the dialog now genuinely retries, which is what the failure copy tells the user to do.
- **Three UI states stopped asserting things no data supports:** a failed run's task rows read `Unknown` rather than `Queued` (the run consumed real time and spend; "Queued" says nothing was attempted), the `aria-live` region is mounted before its first content so "Run started" is actually announced, and an empty goal blocks the `Run` button with a stated reason rather than a keyboard hint.
- **Both route-ordering comments now state the true reason.** The story's Completion Notes recorded that "`/teams/{team_slug}` and `/{run_id}` are both two segments under `/runs`" was wrong (they are two and one, and cannot collide; the real collision is with `/{run_id}/transcript`), but the disproven sentence was left standing verbatim in `api/main.py` and `api/routers/run.py`. A comment is what the next person preserves.

### Left open by the code review

- **Story 2.4 has not had an acceptance audit.** Five of six review layers died on API credit exhaustion, including the Acceptance Auditor; AC 1–16 were never checked. The story is `in-progress`, not `done`, for exactly this reason. Rerunning those layers is the blocker.
- **The "process-wide" run lock is per-`RunRegistry` instance.** `api/runs.py`'s module docstring calls it process-wide, which is true only because `create_app()` is called once in production; `tests/api/conftest.py` builds a fresh app, and therefore a fresh lock, per test. Asserted by comment rather than enforced by code.
- **The `pollEpoch` guard in the Workspace reducer is unreachable.** The polling effect lists `pollEpoch` in its dependency array, so React tears the old effect down (setting `cancelled`) before any render in which the epoch differs, and `cancelled` is what actually discards a superseded poll. The reducer branch and its test remain as harmless redundancy, but neither should be cited as the mechanism.
- **`test_documents_are_never_written_to_disk` cannot catch the regression it names**, because the execution engine — the only component in the run path that could persist document text — is replaced by a stub.
- **The Workspace test harness's "loud on unexpected request" property does not hold on this surface.** The throw happens inside the stubbed `fetch`, which `transport.ts` converts into an `ApiFailure` that the surface's effects then discard. Flagged unverified: this review did not trace `transport.ts`'s catch to confirm.
- **`logger.exception` on a run failure can write attached document text to the server log** if a provider error echoes the offending prompt back — contradicting `api/schemas.py`'s "never persisted, never logged". The generic `failure_reason` is safe; the log is not.
- **The lifespan's shutdown branch is still not in a `try/finally`**, so an exception propagating through `yield` skips the one line that bounds a run thread outliving the app. `_in_flight_thread` is now cleared on a failed start but still not on a successful finish.
- **`web/components/composer/build-result.tsx` does not `encodeURIComponent` its `/teams/${slug}` segment**, while `api-client/keys.ts`'s `sessionPath` documents the opposite rule one directory away. Bounded today by the slug charset, which that code path does not run.
- **`RunView.tasks` is still transmitted for a reason nothing consumes** — `TaskList` renders the mount-time plan and only `RunStatus` reads `run.tasks`, for a count.
- **Attached document text can still spoof the run-context delimiters** injected by `run_context._run_context_block`. Inherent to in-context injection rather than a defect in the implementation, and worth one sentence beside the existing acknowledgement that document text can reach the transcript.
- **`test_the_policy_constants_are_named_not_magic` asserts nothing that can fail** and does not assert `MAX_STORED_RUNS` — the constant the ghost-record finding showed could be exceeded.

## Deferred from: code review of story-2.5 (2026-08-12)

- **`RESERVED_TEAM_NAMES` (`starter`, `example`, `demo`, `template`, `sample`) is a hardcoded guess at Epic 3's starter-team names.** Epic 3 has not started, so there is nothing real to reconcile against yet — whoever ships Epic 3's actual starter teams should revisit this set (and, per a related patch applied in this same review, make sure it also still covers the teams router's own static path segments: `recent`, `browse`, `save`, `rename`). [api/routers/teams.py:111-119]
- **No authentication/authorization exists anywhere on the teams API surface** (save/browse/rename/delete/recent). Consistent with the rest of the `api/` layer's existing local-only design (AD-11: no external services, single local process) — not introduced by this story, and not addressed here.

## Deferred from: code review of story-2.6 (2026-08-14)

- **React `key={provider.name}` assumes provider names are unique per response.** `web/components/settings/settings-surface.tsx:190`. A duplicate name in `providers[]` would silently misrender one row. Defensive-only — the catalog already guarantees unique provider names — but nothing in the frontend contract enforces it.
- **The `cancelled`-flag pattern doesn't abort the in-flight `getKeyStatus()` request on unmount.** `web/components/settings/settings-surface.tsx:85-113`. No `AbortController` is wired in, so the network request keeps running after unmount even though its result is discarded. Mirrors `composer-surface.tsx`'s established pattern, which this story was explicitly instructed to follow — not a new defect, just inherited.
- **Raw error message is shown to the user verbatim on fetch failure; `code`/`fields` on the API result go unused.** `web/components/settings/settings-surface.tsx:96-100`. No differentiation between failure types (e.g. unreachable vs. malformed response). Matches the minimal error-surface convention used elsewhere in the codebase.
- **No retry affordance when the key-status fetch fails.** `web/components/settings/settings-surface.tsx`. The user is left on a static error message with no way to retry short of reloading the page — a real usability gap on a page users may open specifically to diagnose a broken key setup, but outside this story's ACs.

## Deferred from: story-2.8 implementation (2026-08-15)

- **Story 2.5's "prompted to save" UI is still not built** (`deferred-work.md:223`, 2026-08-09,
  re-verified rather than re-recorded — see Story 2.8's Completion Notes). This story's My Teams
  UI is fully functional against the existing backend, but in a real session there is still no way
  for a user to ever get a team into the `teams` DB from the Workspace, so My Teams has nothing to
  show until that prompt exists. Not built here — out of this story's stated scope.
- **`recordTeamRun` fires on every completed run, including a team that was never saved**, and
  is designed to tolerate the resulting `not_found` silently (Story 2.8 AC 3). This is one extra,
  always-failing fetch per completed run for any unsaved team — harmless functionally, but if
  `POST /api/teams/{team_name}/record-run`'s `NOT_FOUND` raise is ever changed to log server-side
  (it is not, today — raised directly, not through `log_and_wrap`), that would become one log line
  per run for the common case of a team nobody has saved. Worth a `WorkspaceSurface`-side "is this
  a saved team" signal if that ever matters; not invented here since nothing today needs it.
- **`web/lib/api-types/run.ts`'s `TeamPlanView` type omits `status: "complete"`**
  (`api/schemas.py:340`), discovered via live verification against the running API, not by reading
  the type definitions side by side. Harmless — the frontend parser ignores unknown fields — and
  outside this story's file list (`run.ts` was not touched), so noted rather than fixed.

## Deferred from: code review of story-2.8 (2026-08-15)

- **Type name inconsistency** — Frontend uses `TeamMessageView` but backend uses `MessageView` [web/lib/api-types/teams.ts:48]. Pre-existing pattern in the codebase, not introduced by this change.
- **No error boundaries in new components** — New components lack error boundaries [web/components/my-teams/*.tsx]. Pre-existing pattern in codebase; no other components use error boundaries.

## Deferred from: code review of story-2.9 (2026-08-15)

- **`env_to_provider()`'s alias registration uses unconditional last-write-wins with no collision detection across providers** [team_maker/adapters/providers/registry.py:141-151]. A future alias colliding with another provider's canonical name/env_var/alias would silently mis-route with no warning. Pre-existing shape (the same last-write-wins already applied to `name`/`env_var`); this diff only extends it to a third field (`env_var_aliases`), and no actual collision exists today given AC4's single-evidenced-alias scope.

## Deferred from: code review of story-3-1 (2026-08-16)

- **Template ID validation missing at schema level** — `template_id` field accepts any string but `get_template()` raises `ValueError` for unknown IDs. No validation at schema level to ensure it matches a registered template. Pre-existing. [team_maker/schema/request.py]
- **Starter YAMLs hardcoded in router** — `_STARTER_YAMLS` list is hardcoded. Adding/removing starter teams requires manual update, creating drift risk with `team_maker/templates/__init__.py` and example files. Pre-existing. [api/routers/starters.py:30-33]
- **No template existence check at request time** — `PipelineRunner._generate_from_template` calls `get_template()` which may fail at runtime if `template_id` references unregistered template. Pre-existing. [pipeline/runner.py:106-107]
- **Agent count mismatch potential** — Starter endpoint uses `len(request.desired_roles)` for `agent_count`, but this reflects the YAML's roles, not the template's actual catalog. Could diverge if YAML roles ≠ template defaults. Pre-existing. [api/routers/starters.py:73]
- **Path traversal risk in starter router** — `_EXAMPLES_DIR` constructed via `Path(__file__).parent.parent.parent / "examples"` with no validation that resolved path stays within repo root. Malicious symlink could escape. Pre-existing. [api/routers/starters.py:27]
- **No validation of YAML data before schema validation** — `_load_starter_yaml` uses `yaml.safe_load` correctly but doesn't verify loaded data structure before validation. Pre-existing. [api/routers/starters.py:46-49]
- **Duplicate template IDs not prevented** — Registry allows overwrites via `@register` decorator pattern. Pre-existing. [templates/registry.py]
- **Thread safety of registry not tested** — No tests for concurrent access to registry or template instances. Out of scope for this story. [tests/unit/templates/test_registry.py]
- **Missing error handling for corrupt YAMLs** — `_get_starter_teams()` logs error and continues on exception, returning partial list. No test verifies partial list behavior. Pre-existing. [api/routers/starters.py:80]
- **No enforcement of YAML filename vs template_id sync** — Starter router assumes `template_id` in YAML matches expected ID. By design, no runtime enforcement. Deferred.
- **Static vs template count divergence in endpoint** — Starter endpoint returns count from YAML's `desired_roles` not template's catalog. Pre-existing. [api/routers/starters.py:73]
- **Missing YAML structure validation** — `_load_starter_yaml` validates schema but doesn't verify that `template_id` matches a registered template or that required fields for starter teams are present. Pre-existing. [api/routers/starters.py]
- **Race condition in registry imports** — `team_maker/templates/__init__.py` imports trigger `@register` decorators, but no ordering guarantee. If imports fail mid-way, registry could be partial. Pre-existing. [templates/__init__.py]
- **No empty starter list test** — If all files in `_STARTER_YAMLS` are missing, `list_starters()` returns empty list. No test verifies this edge case or that frontend handles it gracefully. Test gap. [tests/api/test_starters.py]
- **Path resolution assumes fixed repo structure** — `_EXAMPLES_DIR` assumes fixed relative path from `api/routers/starters.py` to repo root. Packaging or installation could break this. Pre-existing. [api/routers/starters.py:27]
- **No YAML loading failure test** — `test_starters.py` doesn't test behavior when YAML files are missing, corrupt, or have invalid schema. Only happy path tested. Test gap. [tests/api/test_starters.py]
- **Template ID not validated against registry in starter router** — Starter router loads YAMLs directly from filesystem but doesn't verify `template_id` field matches a registered template via `get_template()`. Pre-existing. [api/routers/starters.py]
- **Missing idempotency test for starter teams** — [RESOLVED by Story 3.2] Story 3.1 Dev Notes mention idempotency contract but no test verifies starter team YAMLs produce byte-identical output across runs (like `test_pipeline_is_idempotent`). Out of scope for Task 8, belongs to Story 3.2. Closed by `tests/api/test_starters_run.py::TestStarterRun::test_run_starter_idempotent`.
- **No custom roles test** — No test for starter team with roles not in template's `_ROLE_DEFAULTS`. Out of scope for this story. Deferred.
- **Hardcoded sentinel values in secret containment** — Only tests specific starter IDs (`baseline_education_team`, `research_content_team`) for credential leakage. Pre-existing pattern. [tests/api/test_secret_containment.py:140-142]
- **Missing template_id null test** — No test for starter YAML with missing or null `template_id` field. Test gap. Deferred.
- **No concurrency tests for starter endpoint** — Multiple simultaneous requests to `/api/starters` could race on YAML file reads or registry access. No tests for concurrent access patterns. Out of scope. Deferred.

## Deferred from: code review of story-3.2 (2026-08-16)

- **Missing error handling for corrupt/empty starter YAML in the from-starter compose path** — `yaml.YAMLError`/Pydantic `ValidationError` from `_load_starter_yaml_for_compose` are uncaught in `create_session_from_starter`, would surface as an unhandled 500 and leak the just-created registry entry. Duplicates the same pre-existing gap already logged above against `starters.py`. [api/routers/compose.py:406-410]
- **Missing schema-level validation of `starter_id`** — `CreateSessionFromStarterRequest.starter_id` is an open `str` bounded only by a name-length constant, instead of a `Literal`/enum of the two real starter ids; matches the pre-existing pattern already established in `starters.py`. [api/schemas.py:295]
- **Race condition on concurrent starter builds** — two concurrent `POST /starters/{id}/run` for the same `starter_id` can race on the Factory's non-transactional overwrite-then-write. Pre-existing characteristic of the Factory shared by every build path. [api/routers/starters.py:157-200]
- **`TEAM_MAKER_OUTPUT_ROOT`/CWD assumption in the starter "Run" path** — a non-default output root or a server CWD other than the repo root could make a built starter's `team_slug` unreachable via `output_root()`'s lookup, since the build relies on the starter YAMLs' own hardcoded relative `output_path` matching the default location. A known, explicitly-reasoned simplifying assumption from the story itself, not a new gap. [api/routers/starters.py:157-167]
- **Undisclosed live-network dependency risk in starter-run tests** — `tests/api/test_starters_run.py` doesn't mock provider/model-list calls; inherited from Story 3.1's own `test_starter_teams_build.py` precedent. Verified not to cause failures/flakiness (`pytest -q` full suite: 765 passed, 0 failures), but still an undisclosed live-integration risk per this project's test-transparency rule.
- **Inconsistent success status codes** — `POST /starters/{id}/run` returns 200 despite writing files to disk, while `POST /compose/sessions/from-starter` returns 201 for an in-memory session. Minor REST-convention inconsistency; no AC specifies either code.
- **`tests/shell/routes.test.tsx`'s "Starter Teams page" tests are broken, pre-existing since Story 3.1** — 3 tests ("renders its heading", "renders exactly one plain description sentence", "offers a working primary action") still assert the Story 2.1-era `EmptyState`/`data-slot="empty-title"` stub shape, but Story 3.1 replaced that stub with `StarterTeamsSurface` (a real fetch-driven listing with loading/error/populated states, no `empty-title` in its normal states). Verified against the clean pre-Story-3.2 baseline (`git stash` to the merge-base and re-run): these 3 fail there too, so this is not a Story 3.2 regression — Story 3.1 never updated or removed this file's now-stale "Starter Teams" entry from its `ROUTES` table, unlike `/`'s entry, which its own comment says was migrated out for exactly this reason ("`/` is no longer here... its assertions moved to composer/route.test.tsx"). Out of scope for Story 3.2's review; the fix is either to remove "Starter Teams" from `ROUTES` (its real coverage already lives in `web/tests/starter-teams/starter-teams-surface.test.tsx`) or to update these 3 assertions to match the real surface.
