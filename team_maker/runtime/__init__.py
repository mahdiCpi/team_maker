"""Runtime — executes an already-built Team Package (Story 1.5).

Composer authors, Factory builds, Runtime only executes (AD-5). Nothing in
this package mutates a Team Package on disk or decides team membership/roles.
"""
from __future__ import annotations
